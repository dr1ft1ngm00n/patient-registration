server {
    listen 80;
    server_name localhost;

    # --- 1. Static Frontend UI Routing ---
    location / {
        root /usr/share/nginx/html;
        index index.html;
        # Ensures SPA routing (like React/Vue/custom JS routers) works correctly
        try_files $uri $uri/ /index.html;
    }

    # --- 2. Centralized API Reverse Proxy ---
    # Redirects all /api/ requests to the Docker 'backend' container
    location /api/ {
        # 'his_backend' matches the 'container_name' in your docker-compose.yml
        proxy_pass http://his_backend:3001; 
        
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Cookie $http_cookie;
    }

    # Security Headers
    add_header X-Frame-Options "DENY" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}